// Type declarations for format.js — shared between web and mobile.
export function formatDuration(secs: number): string;
export function formatChapterDuration(secs: number): string;
export function formatPlayerTime(secs: number): string;
