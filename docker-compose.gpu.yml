# GPU override — brings up Whisper (and Kokoro if applicable) with CUDA access.
# Usage on a GPU host (requires nvidia-container-toolkit):
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build whisper
#
# Or to run only the ML services for CLI testing (skip server/nullbore):
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build whisper kokoro

services:
  whisper:
    build:
      context: ./services/whisper
      dockerfile: Dockerfile.gpu
    environment:
      - WHISPER_MODEL=large-v3
      # auto: use the GPU when the CUDA runtime is actually usable, else fall
      # back to CPU int8 at load time (a mismatched/absent driver degrades to
      # CPU instead of crash-looping the container).
      - WHISPER_DEVICE=auto
      # Blank so server.py picks the compute type for the resolved device
      # (float16 on cuda, int8 on cpu). Without this the base compose's
      # WHISPER_COMPUTE_TYPE=int8 leaks through and pins the GPU to int8.
      - WHISPER_COMPUTE_TYPE=
    ports:
      - "5200:5200"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
