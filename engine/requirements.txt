# DEPRECATED (#57). Split into:
#   requirements-base.txt  — CPU-capable deps (every platform; torch installed
#                            separately by build.sh, variant-pinned)
#   requirements-cuda.txt  — cuBLAS/cuDNN wheels for CTranslate2 GPU (NVIDIA only)
#
# build.sh picks the variant (cpu default / cuda when an NVIDIA GPU is detected)
# so CPU/Mac hosts never pull torch+CUDA. This file is kept only so older refs
# don't 404; do not add deps here.
