# Compiled binaries
/abookify
/bin/
/stt-cli
/tts-cli

# Syncthing temp / metadata files
.syncthing.*
.stversions/

# Local runtime data (SQLite database + WAL files)
/data/
*.db
*.db-journal
*.db-shm
*.db-wal

# Generated audio/transcripts produced by the TTS/STT pipeline
/generated/

# Test library — large audiobook + ebook files stored outside the repo.
# Set ABOOKIFY_LIBRARY_PATH or symlink testdata/library to point at yours.
/testdata/library/

# Environment / secrets
.env
.env.local
.env.*.local

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Desktop-bundle server binaries (make build-server)
dist/
