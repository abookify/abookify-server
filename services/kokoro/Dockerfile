# Kokoro-FastAPI provides a much higher quality TTS than Piper
# Pre-built image with OpenAI-compatible Speech endpoint
# CPU: ghcr.io/remsky/kokoro-fastapi-cpu:latest
# GPU: ghcr.io/remsky/kokoro-fastapi-gpu:latest
#
# We use a thin wrapper Dockerfile so we can pin the version
# and add any customization later.

FROM ghcr.io/remsky/kokoro-fastapi-cpu:latest
